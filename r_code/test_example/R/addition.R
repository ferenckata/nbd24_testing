#' Adding two numbers
#' 
#' @param x Number one
#' @param y Number two
#' @returns The sum of two numbers
#' @examples
#' add_two_numbers(1, 1)
add_two_numbers <- function(x, y) {
    return(x + y)
}